# Copy these keys into Vercel → Project → Settings → Environment Variables.
# Real values live in .env.local (gitignored), never committed here.

VITE_GA4_MEASUREMENT_ID=
VITE_CLARITY_PROJECT_ID=
VITE_RECAPTCHA_SITE_KEY=
